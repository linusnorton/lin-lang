[package]
name = "lin-ir"
version = "0.1.0"
edition = "2021"

[dependencies]
lin-common = { path = "../lin-common" }
lin-check = { path = "../lin-check" }
lin-parse = { path = "../lin-parse" }
indexmap = { workspace = true }
